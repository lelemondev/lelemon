# Railway configuration for Lelemon monorepo
# Each service is configured separately in Railway dashboard
# This file provides defaults

[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
