# Lelemon Environment Variables

# Database (Supabase PostgreSQL)
DATABASE_URL=postgresql://postgres:password@localhost:5432/lelemon

# API
PORT=4000
NODE_ENV=development

# Auth (for dashboard)
JWT_SECRET=your-jwt-secret-here
